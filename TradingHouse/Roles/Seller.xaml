<Window x:Class="TradingHouse.Roles.Seller"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Продавец" Height="450" Width="800">

    <DockPanel>
        <ToolBar DockPanel.Dock="Top" FontSize="20">
            <ToolBar.Resources>
                <Style x:Key="{x:Static ToolBar.ButtonStyleKey}" TargetType="Button">
                    <Setter Property="Margin" Value="5 0" />
                </Style>
            </ToolBar.Resources>

            <Button Name="selling_button" Content="Продажи отдела" Click="selling_button_Click" />
            <Button Name="product_button" Content="Товары отдела" Click="product_button_Click" />
            <Button Name="logout_button" Content="Выйти" Click="logout_button_Click" />
        </ToolBar>

        <Frame Name="frame" NavigationUIVisibility="Hidden" />
    </DockPanel>
</Window>